import 'package:flutter/material.dart';

class LoginInScreen extends StatelessWidget {
  static String id = "LoginInScreen";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
